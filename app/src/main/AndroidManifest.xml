<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.RedmandRobotTest.InstagramCollage">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name="com.RedmandRobotTest.InstagramCollage.application.InstagramCollageApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <provider
            android:name=".Provider.FileProvider"
            android:authorities="com.RedmandRobotTest.InstagramCollage.Providers.FileProvider"
            android:exported="true" />

        <activity
            android:name=".Activity.ImagesActivity"
            android:label="@string/app_name"
            android:windowSoftInputMode="stateUnchanged|adjustResize">

            <intent-filter>

                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>

            </intent-filter>

        </activity>

    </application>

</manifest>
